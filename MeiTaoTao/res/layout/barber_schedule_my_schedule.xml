<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/body_backcolor"
    android:orientation="vertical" >

    <include layout="@layout/title_bar" />

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="@dimen/layout_height8"
        android:background="@drawable/signup_location_bg" >

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:orientation="vertical" >

            <TextView
                android:id="@+id/tv_already_schedule"
                style="@style/indicator_text_style"
                android:text="@string/already_schedule"
                android:textColor="@color/tab_text_top_selecter" />

            <ImageView
                android:id="@+id/iv_already_schedule"
                android:layout_width="@dimen/image_width2"
                android:layout_height="1.5dp"
                android:layout_gravity="center_horizontal"
                android:scaleType="fitXY"
                android:src="@drawable/tab_image_top_selecter"
                android:textSize="14sp" />
        </LinearLayout>

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:orientation="vertical" >

            <TextView
                android:id="@+id/tv_add_schedule"
                style="@style/indicator_text_style"
                android:text="@string/add_schedule"
                android:textColor="@color/tab_text_top_selecter" />

            <ImageView
                android:id="@+id/iv_add_schedule"
                android:layout_width="@dimen/image_width2"
                android:layout_height="@dimen/image_height2"
                android:layout_gravity="center_horizontal"
                android:scaleType="fitXY"
                android:src="@drawable/tab_image_top_selecter"
                android:textSize="14sp" />
        </LinearLayout>
    </LinearLayout>

    <android.support.v4.view.ViewPager
        android:id="@+id/viewPage"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

</LinearLayout>